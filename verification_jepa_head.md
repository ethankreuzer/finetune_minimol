# Transformer-head verification

Generated by `src/verify_jepa_head.py` on 2026-09-01 19:34:56.

`nn.TransformerEncoder` is permutation-equivariant, so a head over Mol-JEPA's 13
tokens can silently ignore which token is which. These are measurements that it
does not.

| Check | Result | Measurement |
|---|---|---|
| permuting tokens changes the output | PASS | max\|delta pred\| = 7.657e-03 under a random token permutation |
| the permutation check has teeth | PASS | with token_embed zeroed, the same permutation moves the output by 4.172e-07 -- so a live reading is the position parameter, not noise |
| gradient reaches every parameter | PASS | token_embed grad max\|g\| = 1.421e-02; 0 parameters with grad None |
| mean and max pooling both run, and differ | PASS | max\|delta pred\| between poolings = 6.537e-01 at identical weights |
| token-shape contract is enforced | PASS | flat [B, D]: rejected; wrong token count: rejected |
| the five loss terms are finite and non-zero | PASS | cls 0.6259, huber 0.5113, pair 1.0984, std 0.5042, vic 0.6956 |

## Notes

- **permuting tokens changes the output** — if this were 0 the head would be treating the 13 modalities as an unordered bag, and would still train and score plausibly
- **the permutation check has teeth** — pairs with the check above exactly as check_grad_flow is paired: a tolerant test never seen to reject anything tests nothing
- **gradient reaches every parameter** — a token_embed with no gradient would sit at its init forever, which is the dead-position case in slow motion
- **mean and max pooling both run, and differ** — sweeping a knob that does nothing would waste half the trials
- **token-shape contract is enforced** — the MLP arm's flattened readout and this head's sequence are both 'the cache'; only one is what this head means
- **the five loss terms are finite and non-zero** — a float64 slice or a collapsed token shows up here and nowhere else

**Overall: PASS**

