# Mol-JEPA embedding cache verification

Generated by `src/verify_jepa_embed.py` on 2026-09-01 15:12:10.

The cache is computed once and every number on this branch is a function of it, so
each claim below is a measurement rather than an assertion -- per the repo
convention (CLAUDE.md "Conventions").

## Environment

- `cache`: data/embeddings/moljepa_v1
- `molecules`: 331,480
- `arrays`: embeddings (331480, 13, 512), projected (331480, 13, 512)
- `hf revision`: 4c912b450175f31b5ba913a5dc921c03b27b985a
- `cache built on`: cuda (repeat max|delta| 0.0)
- `verified on`: cpu
- `torch`: 2.6.0+cu124
- `transformers`: 4.57.6

## Results

| Check | Result | Measurement |
|---|---|---|
| model matches the frozen reference | PASS | max\|delta\| embeddings 0.000e+00, projected 0.000e+00 over 64 molecules |
| all Dropout in eval mode | PASS | 29/29 Dropout modules with training=False |
| reproducible on cpu | PASS | max\|delta\| repeat @64 0.0e+00, batch 8 0.0e+00, batch 16 0.0e+00, batch 256 0.0e+00, reversed 0.0e+00; batch COMPOSITION (reported, not asserted) 5.3e-07 |
| token layout re-derives | PASS | 13 tokens, cls at index 0: matches meta.json |
| cached row i == fresh(CSV row i) | PASS | in-chunk on cuda: max\|delta\| embeddings 0.000e+00, projected 0.000e+00 at rows [0, 1, 3152, 165740, 331478, 331479]. An off-by-one gives 1.164e+00 and a scattered re-embed 7.5e-07, so the check separates a shift from float noise by ~1547188x |
| loader guards have teeth | PASS | changed CSV sha256: rejected; --limit cache: rejected; readout 'emb:nope': rejected; readout 'pred:graph': rejected; readout 'nonsense': rejected |
| finite, and no collapsed token | PASS | 0 non-finite values; smallest per-dimension (max-min) 2.494e-03 (at embeddings:uma) |
| 5 val folds tile the cache exactly once | PASS | 331,480/331,480 rows covered once, 0 uncovered, 0 duplicated; max index 331,479 |

## Notes

- **model matches the frozen reference** — fixture from Flogrammer/Mol-JEPA@4c912b450175; catches a changed Hub revision, which trust_remote_code makes possible
- **all Dropout in eval mode** — a single live Dropout would make every cached row a random draw
- **reproducible on cpu** — re-chunking is the freedom a rebuild actually has, and it is bit-exact. Composition is not invariant on either device, so a rebuilt cache matches bit-for-bit only at the same --batch on the same device
- **token layout re-derives** — cls is modality_pred[0](embeddings[:,0]), not a slice of it -- check_layout asserts that relationship bit-exactly
- **cached row i == fresh(CSV row i)** — two-sided: bit-exact against the right row, and shown to reject the neighbouring one -- a tolerant check that never rejects anything tests nothing
- **loader guards have teeth** — each guard is shown rejecting something, not merely present
- **finite, and no collapsed token** — a zero-variance token would train and score without ever erroring
- **5 val folds tile the cache exactly once** — this is what lets pooled out-of-fold metrics use all 100 potent molecules rather than 20

**Overall: PASS**

